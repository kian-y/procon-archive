#include <bits/stdc++.h>
#define rep(i, n)  for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main(){
  int a,b,c;
  string str;
  cin >> a >> b >> c >> str;
  cout << a+b+c << " " << str << endl;
}
